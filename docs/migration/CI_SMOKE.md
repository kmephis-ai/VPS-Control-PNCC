# CI Smoke Probe

Purpose: trigger the first pull-request execution of the `public-safety` workflow after bootstrap installation on `main`.

This file contains no product source and no runtime/instance data. It may be removed after the CI baseline is proven.

Retrigger: 2026-08-26T19:35+03:00 after repository Actions state was confirmed `enabled=true`, `allowed_actions=all`, workflow `public-safety` active.
