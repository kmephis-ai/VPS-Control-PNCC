# RC14.38 sanitized public source import

Status: MIGRATION INPUT / NOT RUNTIME QUALIFIED

## Provenance

The imported directory `legacy/v7-rc14.38-sanitized/` is derived from the exact private
`VPS-Control-v7.0.0-rc14.38.zip` candidate whose SHA-256 is:

`6d81137519a363ebf3d8503f33a344d8fdc75848d517cf732cb6d6d02394d727`

The public snapshot is intentionally **not byte-identical** to that candidate because publication
sanitation occurred before Git history.

## Sanitation boundary

Before public import:
- owner VPS exit address was replaced with a TEST-NET example;
- owner router address was replaced with a TEST-NET example;
- owner PuTTY saved-session name was replaced with a generic example;
- owner-specific cloud-drive example path was replaced with a generic example;
- the original package per-file checksum list was removed because sanitation changes bytes;
- a new sanitized checksum manifest was generated.

Not imported:
- credentials/password files/DPAPI blobs/private keys;
- runtime logs/evidence;
- PID/heartbeat/state;
- private PuTTY profile data;
- local Git history;
- third-party binaries.

## Runtime status

This import does not promote RC14.38 and does not resolve its ongoing runtime investigation.
`CI VERIFIED` for this branch means only that the sanitized public engineering tree passed hosted
checks. `RUNTIME VERIFIED` still requires a separately approved exact runtime artifact and fresh
trusted Windows evidence.

## Legacy credential debt

The sanitized legacy snapshot may contain historical plaintext `-pw` implementation paths in
legacy management code. These are preserved as migration evidence only. New product source outside
`legacy/` is blocked by CI from introducing literal PuTTY `-pw`; the target product contract is
DPAPI-at-rest and PuTTY `-pwfile` only.
