name: stable-watchdog-lifecycle-v2

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  stable-watchdog-lifecycle-v2:
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false

      - name: PowerShell 5.1 parse and BOM preflight
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          $path='tools/runtime-agent/Test-PnccStableWatchdogLifecycleV2.ps1'
          $bytes=[IO.File]::ReadAllBytes((Resolve-Path $path))
          if($bytes.Length -lt 3 -or $bytes[0]-ne 0xEF -or $bytes[1]-ne 0xBB -or $bytes[2]-ne 0xBF){throw 'UTF8 BOM required'}
          $tokens=$null;$errors=$null
          [Management.Automation.Language.Parser]::ParseFile((Resolve-Path $path),[ref]$tokens,[ref]$errors)|Out-Null
          if(@($errors).Count){$errors|ForEach-Object{Write-Error $_};exit 1}
          'STABLE_WATCHDOG_LIFECYCLE_V2_PARSE=PASS PS51=true'

      - name: Assert engine-native heartbeat contract boundary
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          $text=Get-Content tools/runtime-agent/Test-PnccStableWatchdogLifecycleV2.ps1 -Raw
          foreach($required in @('PNCC_STABLE_WATCHDOG_LIFECYCLE_V2','heartbeat Pid does not match registered Watchdog','watchdog heartbeat stale by engine-native Timestamp','post_start_heartbeat_must_bind_new_pid','post_start_timestamp_must_be_after_start','runtime_authority=$false','promotion_eligible=$false')){if(-not$text.Contains($required)){throw "required V2 contract marker missing: $required"}}
          foreach($forbidden in @('Stop-Process','taskkill','RestartTunnel','Restart-Service','Stop-Service','Start-Service','-Action StartReserve','-Action StopReserve','SelectReserveRoute')){if($text -match [regex]::Escape($forbidden)){throw "forbidden mutation primitive in V2 read-only contract: $forbidden"}}
          if($text -match '(?i)(?:^|\s)-pw(?:\s|=)'){throw 'plaintext -pw marker forbidden'}
          'STABLE_WATCHDOG_LIFECYCLE_V2_BOUNDARY=PASS READ_ONLY=true'

      - name: Execute admitted V2 fixture under Windows PowerShell 5.1
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          $out=Join-Path $env:RUNNER_TEMP 'stable-watchdog-lifecycle-v2'
          New-Item -ItemType Directory -Force -Path $out|Out-Null
          powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File .\tools\runtime-agent\Test-PnccStableWatchdogLifecycleV2.ps1 -Mode Fixture -FixturePath .\.pncc-dev\tests\stable-watchdog-lifecycle-v2-ready.fixture.json -OutputDirectory $out
          if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}
          $r=Get-Content (Join-Path $out 'stable-watchdog-lifecycle-v2-result.json') -Raw|ConvertFrom-Json
          if($r.contract_id -ne 'PNCC_STABLE_WATCHDOG_LIFECYCLE_V2' -or $r.state -ne 'WATCHDOG_LIFECYCLE_V2_CONTRACT_ADMITTED'){throw 'V2 fixture contract/state mismatch'}
          if([bool]$r.physical_execution_allowed -or [bool]$r.runtime_mutation -or [bool]$r.runtime_authority -or [bool]$r.promotion_eligible){throw 'V2 fixture authority/mutation invariant failed'}
          'STABLE_WATCHDOG_LIFECYCLE_V2_FIXTURE=PASS'

      - name: Assert repository remains clean
        shell: powershell
        run: |
          if(git status --porcelain --untracked-files=all){git status --short;throw 'repository became dirty'}
