name: stable-runtime-authority-owner-grant

on:
  pull_request:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  waiting-boundary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - name: Evaluate default-deny Owner grant boundary
        run: python3 .pncc-dev/scripts/evaluate_stable_runtime_authority_owner_grant.py
      - name: Assert no authority or release side effects
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          from pathlib import Path
          d=json.load(open('.pncc-dev/attestations/stable-runtime-authority-owner-grant-v7.0.0.json',encoding='utf-8'))
          assert d['grant_state']=='WAITING_OWNER_AUTHORIZATION'
          assert d['owner_authorization_present'] is False
          assert d['runtime_authority'] is False
          assert d['promotion_eligible'] is False
          assert d['release_or_tag_authorized'] is False
          assert d['tag_created'] is False
          assert d['release_created'] is False
          assert d['runtime_mutation'] is False
          s=Path('.github/workflows/stable-runtime-authority-owner-grant.yml').read_text(encoding='utf-8')
          assert sum(1 for line in s.splitlines() if line.strip() == 'contents: read') == 1
          forbidden=['git'+' push','git'+' tag','gh'+' release','contents:'+' write','releases:'+' write']
          for token in forbidden:
              assert token.lower() not in s.lower(), token
          PY
      - name: Assert repository remains clean
        run: test -z "$(git status --porcelain)"
