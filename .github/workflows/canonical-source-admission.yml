name: canonical-source-admission

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  canonical-source-admission:
    name: canonical-source-admission
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
          fetch-depth: 0

      - name: Verify canonical Git-blob provenance and package manifest
        shell: python
        run: |
          from pathlib import Path
          import hashlib,json,subprocess,re
          declaration=json.loads(Path('.pncc-dev/candidate-source.json').read_text(encoding='utf-8-sig'))
          version=declaration.get('candidate_version')
          roots=declaration.get('source_roots')
          if not isinstance(version,str) or not version: raise SystemExit('candidate version missing')
          if not isinstance(roots,list) or len(roots)!=1 or not isinstance(roots[0],str): raise SystemExit('exactly one canonical source root required')
          root=roots[0]
          prov_value=declaration.get('provenance_path')
          if not isinstance(prov_value,str) or not prov_value: raise SystemExit('current provenance path missing')
          prov_path=Path(prov_value)
          if not prov_path.is_file(): raise SystemExit('current canonical provenance missing')
          prov=json.loads(prov_path.read_text(encoding='utf-8-sig'))
          if prov.get('hash_semantics')!='CANONICAL_GIT_BLOB_BYTES': raise SystemExit('unexpected hash_semantics')
          if prov.get('source_root')!=root: raise SystemExit('unexpected source_root')
          inv=prov.get('inventory',[])
          if not isinstance(inv,list) or not inv: raise SystemExit('provenance inventory missing')
          def blob(path): return subprocess.check_output(['git','show',f'HEAD:{path}'])
          actual_paths=[]
          for rec in inv:
              if not isinstance(rec,dict) or set(rec)!={'bytes','path','sha256'}: raise SystemExit('malformed provenance row')
              rel=rec['path']; b=blob(root+'/'+rel)
              if len(b)!=rec['bytes'] or hashlib.sha256(b).hexdigest()!=rec['sha256']:
                  raise SystemExit('provenance Git-blob mismatch: '+rel)
              actual_paths.append(rel)
          tracked=subprocess.check_output(['git','ls-files',root],text=True).splitlines()
          expected=[Path(x).relative_to(root).as_posix() for x in tracked]
          if sorted(actual_paths)!=sorted(expected): raise SystemExit('provenance inventory coverage mismatch')
          manifest=blob(root+'/VPS-Control-v7-SHA256.txt').decode('utf-8-sig').splitlines()
          first=next((x.strip() for x in manifest if x.strip()),'')
          if version not in first: raise SystemExit('manifest current-version heading mismatch')
          rows=[x for x in manifest if x and not x.startswith('#')]
          expected_manifest=sorted(x for x in expected if x!='VPS-Control-v7-SHA256.txt')
          if len(rows)!=len(expected_manifest): raise SystemExit(f'expected {len(expected_manifest)} package manifest entries, got {len(rows)}')
          manifest_paths=[]
          for row in rows:
              m=re.match(r'^([0-9a-f]{64})  (.+)$',row)
              if not m: raise SystemExit('malformed manifest row')
              rel=m.group(2).replace('\\','/')
              b=blob(root+'/'+rel)
              if hashlib.sha256(b).hexdigest()!=m.group(1): raise SystemExit('package manifest mismatch: '+rel)
              manifest_paths.append(rel)
          if sorted(manifest_paths)!=expected_manifest: raise SystemExit('package manifest coverage mismatch')
          base=prov.get('baseline',{})
          if base.get('embedded_version')!=version or base.get('activated_candidate_version')!=version: raise SystemExit('activated candidate version provenance invalid')
          if base.get('requires_version_bump_before_build') is not False: raise SystemExit('version bump boundary not cleared')
          safety=prov.get('safety',{})
          if safety.get('build_input_ready') is not True or safety.get('artifact_exists') is not False: raise SystemExit('build-input/artifact truth invalid')
          if any(safety.get(k) for k in ('runtime_authority','promotion_authority','stable_done')): raise SystemExit('provenance grants forbidden authority')
          print(f'CANONICAL_GIT_BLOB_PROVENANCE=PASS FILES={len(inv)} MANIFEST={len(rows)} VERSION={version} ARTIFACT_EXISTS=false PROVENANCE={prov_path.as_posix()}')

      - name: Verify Windows PowerShell 5.1 AST, BOM and credential transport
        shell: powershell
        run: |
          $ErrorActionPreference='Stop'
          if($PSVersionTable.PSVersion.Major -ne 5 -or $PSVersionTable.PSVersion.Minor -ne 1){throw "Windows PowerShell 5.1 required; got $($PSVersionTable.PSVersion)"}
          $root=Join-Path $env:GITHUB_WORKSPACE 'src\windows-v7'
          $files=@(Get-ChildItem -LiteralPath $root -Filter '*.ps1' -File -Recurse)
          if($files.Count -ne 23){throw "Expected 23 PS1 files, got $($files.Count)"}
          $errors=New-Object Collections.ArrayList
          foreach($file in $files){
            $tokens=$null;$parseErrors=$null
            [void][System.Management.Automation.Language.Parser]::ParseFile($file.FullName,[ref]$tokens,[ref]$parseErrors)
            foreach($e in @($parseErrors)){[void]$errors.Add("AST $($file.FullName):$($e.Extent.StartLineNumber): $($e.Message)")}
            $bytes=[IO.File]::ReadAllBytes($file.FullName)
            $bom=$bytes.Length -ge 3 -and $bytes[0]-eq 0xEF -and $bytes[1]-eq 0xBB -and $bytes[2]-eq 0xBF
            if(-not $bom){[void]$errors.Add("UTF8_BOM_REQUIRED: $($file.FullName)")}
          }
          if($errors.Count -gt 0){$errors|ForEach-Object{Write-Error $_};throw "Canonical PS5.1 gate failed: $($errors.Count)"}
          python .pncc-dev/scripts/check_putty_password_transport.py --repository-root .
          if($LASTEXITCODE -ne 0){throw "Credential transport scanner failed rc=$LASTEXITCODE"}
          Write-Host "CANONICAL_PS51=PASS AST=23 BOM=23 EXECUTABLE_PLAINTEXT_PW=0"

      - name: Verify fixed tunnel, version and credential boundaries
        shell: python
        run: |
          from pathlib import Path
          import json
          declaration=json.loads(Path('.pncc-dev/candidate-source.json').read_text(encoding='utf-8-sig'))
          version=declaration['candidate_version']
          roots=declaration['source_roots']
          if len(roots)!=1: raise SystemExit('exactly one canonical source root required')
          root=Path(roots[0])
          c=json.loads((root/'VPS-Control-v7-TUNNEL-CONTRACT.json').read_text(encoding='utf-8-sig'))
          if c.get('ContractVersion')!=version: raise SystemExit('tunnel ContractVersion mismatch')
          by={x['Id']:x for x in c['Tunnels']}
          p=by['PRIMARY_AUTO']; r=by['RESERVE_MANUAL']
          assert p['Port']==1081 and p['LifecycleMode']=='AUTO' and p['AutoStart'] and p['AutoRecovery']
          assert r['Port']==1080 and r['LifecycleMode']=='MANUAL_ONLY' and not r['AutoStart'] and not r['AutoRecovery'] and not r['AutoStop']
          assert c['CredentialTransport']['PuttyArgument']=='-pwfile' and not c['CredentialTransport']['PlaintextPwArgumentAllowed']
          assert not c['ManagedPuttyHostKeyTrust']['HostKeyVerificationDisableAllowed']
          main=(root/'VPS-Control-v7.ps1').read_text(encoding='utf-8-sig')
          launch=(root/'VPS-Control-v7-launch.ps1').read_text(encoding='utf-8-sig')
          assert f"$UiVersion = '{version}'" in main
          assert f"$LauncherVersion = '{version}'" in launch
          print(f'CANONICAL_FIXED_BOUNDARIES=PASS PRIMARY=1081 RESERVE=1080 PWFILE=true VERSION={version}')
